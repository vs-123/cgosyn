package hello_world

import (
	"stdio.h"
	"math.h"
)

func main() int {
	var age int = 21

	if isAdult(age) {
		printf("You are an adult!")
	} else {
		printf("You are younger than 18!")
	}

	return 0
}

func isAdult(age int) bool {
	return age >= 18
}
